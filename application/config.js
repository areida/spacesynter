'use strict';

module.exports = {
    api : {
        hostname : window.location.hostname,
        port     : window.location.port,
        prefix   : '/api'
    },
    auth      : false,
    login_url : '/login'
};
'use strict';

export default {
    api : {
        hostname : window.location.hostname,
        port     : window.location.port,
        prefix   : '/api'
    },
    auth      : false,
    login_url : '/login'
};